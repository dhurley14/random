Run the Provider class first by typing the following into a terminal:

javac Provider.java
java Provider

Then run the Requester class in the same manner.  The Provider run up to 10 different Requesters at a time.